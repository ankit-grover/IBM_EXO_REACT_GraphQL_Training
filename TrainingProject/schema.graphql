type Query {
        employees: [Employee]
}

type Employee {
    id: ID!,
    name: String!,
    skills: [Skills],
    resignationDate: String
}

type Skills {
    id: String!
    skill: String
}

type Mutation {
    createEmployee(name: String!, resignationDate: String): Employee
}

